# AI 직무 롤플레이 시뮬레이터: 실서비스와 데모를 분리한 LLM 연습 도구

[English](PORTFOLIO.md) | [한국어](PORTFOLIO.ko.md)

## 프로젝트 개요

고객 응대, 면접, 영업 대화는 반복 연습이 필요하지만 매번 적절한 상대를 구하기 어렵다. 이 프로젝트는 사용자가 상황과 난이도를 고르면 LLM이 대화 상대가 되고, 대화가 끝나면 강점·개선점·네 가지 점수를 포함한 피드백을 돌려주는 Next.js 애플리케이션이다.

## 설계에서 중요했던 점

핵심은 API 키가 없는 사람도 흐름을 확인할 수 있게 하면서, 실제 LLM과 대화한 기록을 데모 데이터와 섞지 않는 것이었다. 그래서 `/api/chat`과 `/api/feedback`은 `OPENROUTER_API_KEY`가 있을 때만 OpenRouter 호환 모델을 호출하고, 없으면 체크인된 데모 응답을 사용한다. 실제 모드에서는 Supabase가 세션·메시지·피드백을 저장하고 대시보드에 누적값을 보여주지만, 데모 모드의 결과는 `sessionStorage`에만 남는다.

## 구현 흐름

세 가지 직무 범주와 세 난이도로 구성된 아홉 개 시나리오는 `lib/personas.ts`에 모았다. 클라이언트는 시나리오 선택, 세션 생성, 역할극, 피드백, 대시보드 화면을 나눠 맡는다. 서버 라우트는 세션 UUID를 만들고, 대화 이력과 시나리오 프롬프트를 LLM에 전달한 뒤, 구조화된 피드백을 요청한다. 피드백에는 총점·공감·문제 해결·커뮤니케이션 점수가 있지만, 점수 자체를 제품 기능의 진실성처럼 보이지 않게 데모 경로도 분리했다.

## 결과와 해석

저장소는 자격증명 없이도 역할극→피드백까지 탐색할 수 있는 화면 흐름과, 자격증명 설정 시 대화 이력을 저장하는 경로를 구현한다. 이는 LLM 기능을 보여주는 데는 충분하지만, 사용자 수·지연 시간·배포 상태·코칭 효과에 대한 수치를 보관한 것은 아니다.

## 한계와 다음 단계

현재 LLM 점수는 연습용 피드백이지 채용·교육 평가가 아니다. 점수 체계가 유용하다고 주장하려면 사람 평가자와의 일치도, 프롬프트별 편향, 재현성 검증이 필요하다. Supabase 스키마와 RLS 정책도 저장소에 버전 관리돼 있지 않아, 실제 서비스 운영 전에는 권한 정책과 통합 테스트를 추가해야 한다.

## 근거

- 구현 개요: [`README.md`](README.md)
- 구조: [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md)
- 라우트: `app/api/session`, `app/api/chat`, `app/api/feedback`

## 제품 흐름을 끊기지 않게 만든 방법

사용자는 직무 범주와 난이도를 고른 뒤 역할극 세션을 시작하고, 대화를 주고받은 후 피드백 화면으로 이동한다. `lib/personas.ts`에 모은 3개 직무 범주와 3개 난이도의 9개 시나리오는 이 흐름에서 프롬프트의 기준점이 된다. 서버는 세션 UUID를 먼저 만들고, 이후 메시지 이력과 선택한 persona를 함께 전달해 대화 맥락이 매 턴 사라지지 않게 한다. 피드백 단계에서는 대화 전문을 바탕으로 강점·개선점과 총점·공감·문제 해결·커뮤니케이션 네 점수를 구조화해 받도록 설계했다.

가장 중요한 제품 판단은 실제 생성 결과와 데모 결과를 섞지 않은 것이다. `OPENROUTER_API_KEY`가 있을 때만 OpenRouter 호환 LLM을 호출하며, 없는 환경에서는 체크인된 응답으로 역할극→피드백의 화면 흐름을 보여 준다. 실제 모드의 세션·메시지·피드백은 Supabase를 통해 누적되지만, 데모 모드는 브라우저 `sessionStorage`에만 남는다. 이 경계가 없으면 예시 대화가 실제 사용자 기록처럼 보이거나, 키가 없는 포트폴리오 검토자가 앱 자체를 볼 수 없게 된다.

## 결과를 읽는 기준

현재 저장소가 보여 주는 결과는 ‘LLM 기반 연습 경험을 한 흐름으로 연결한 앱’이다. 9개 시나리오, 세션 API, 대화 API, 피드백 API, 실제/데모 분기와 대시보드 화면은 구현돼 있지만, 배포 URL·활성 사용자·대화 지연 시간·피드백 만족도는 보관돼 있지 않다. 특히 네 가지 점수는 코칭 대화를 정리하는 인터페이스이며 채용 판단이나 교육 성취도를 측정한 척도가 아니다. 다음에는 Supabase 스키마와 RLS 정책을 버전 관리하고, 사람 평가와 LLM 피드백의 일치도 및 편향을 검증해야 한다.
